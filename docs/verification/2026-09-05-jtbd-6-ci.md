# JTBD-6 — Continuous integration verification

Date: 2026-09-05

## Workflow

`.github/workflows/ci.yml` runs on every push and pull request with:

- Node.js 22;
- `npm ci` from `packages/cloud`;
- `npm test`;
- `npm run build`;
- read-only repository permission and no AWS credentials, secrets, or deployment step.

The root README badge is:

<https://github.com/stantheman070911/futuremode/actions/workflows/ci.yml/badge.svg>

The linked workflow page is:

<https://github.com/stantheman070911/futuremode/actions/workflows/ci.yml>

## Local parity check

Before push, the same package commands passed locally: 53/53 tests and TypeScript build.
The workflow run on `main` must be green before this job is considered complete.
